/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // allowedDevOrigins: ['http://192.168.100.12:3000'], // or whatever your LAN IP + port is
  },
};

module.exports = nextConfig;
